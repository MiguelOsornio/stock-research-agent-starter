# Web service
PORT=
SHOW_PUBLIC_HEADER=false
RESEARCH_DELAY_MS=4000
RENDER_API_KEY=
WORKFLOW_SERVICE_SLUG=
MODEL_BASE_URL=https://api.openai.com/v1
MODEL_NAME=gpt-4o-mini
MODEL_API_KEY=
WORKSHOP_TOKEN=

# Workflow service
# Set RENDER_WORKFLOW=1 on the Workflow service (not the web service).
RENDER_WORKFLOW=
NEWS_FAIL_ENDPOINT=
# Copy MODEL_* and WORKSHOP_TOKEN onto the Workflow if the web service uses them.
