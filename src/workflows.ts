/**
 * Starter placeholder. The tutorial adds the real task() wrapper here.
 * Workflow start command points at this file so the completed path can replace it.
 */
console.log(
  "No Workflow tasks registered yet. Follow the tutorial to add researchStock.",
)

// Keep the process alive so a mistaken Workflow deploy fails loudly in logs
// instead of exiting 0 with nothing registered.
setInterval(() => {}, 1 << 30)
